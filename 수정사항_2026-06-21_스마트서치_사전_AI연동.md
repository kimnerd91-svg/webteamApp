# 스마트서치 + 의학용어사전 AI 연동 — 2026-06-21

> 대상: 메디앤메디 치과 사이트 자체 통합검색·용어사전 시스템 (PHP/Gnuboard/Cafe24)
> 기준 사이트: 서울상록수치과
> 폴더: `/sub/smartsearch/`

---

## 0. 한 줄 요약

스마트서치 경로 관리에 **Gemini AI 자동 채우기**를 달아, 진료명 하나로 유의어·위키 설명·증상 단서를 한 번에 생성.
사전(dm_dict)과 직접 연결하는 대신 **AI가 경로 보고 직접 생성**하는 방식으로 단순화.
증상 검색은 느슨하게(이빨=이=치아) + 애매하면 "어디에 가까우세요?" 후보 안내.
검색창 자동완성 미리보기 + 용어사전 사용자 프론트(dict.php) 신규.

---

## 1. 핵심 설계 결정

- **사전 직접 연결 → AI 직접 생성으로 전환**: 처음엔 dm_dict.related를 경로에 역인덱스로 머지하려 했으나, "AI로 모든 칸 자동 생성"이 더 깔끔 → 사전-스마트서치 매칭 의존 제거. (dm_dict 연동 코드는 무해하게 잔존: related 있는 용어만 빌드 시 머지, 없으면 스킵)
- **AI 한 번에 = 유의어 + 위키 + 증상 단서**: 탭 왕복 없이 경로 카드 하나에서 검색어·위키·증상규칙까지 생성·저장.
- **증상 매칭은 느슨하게**: 빡빡한 정확 일치 대신 부위어·통증어 정규화(이빨=이=치아, 아파요=아파). 애매하면 단독 확정 대신 후보 여러 개 안내.
- **DB 일원화**: search_supplement.php / search_symptoms.php 파일 의존 완전 제거 → dm_ss_keyword / dm_ss_symptom (DB)만 사용.

---

## 2. 파일별 수정 내역

### smartsearch_manager.php (관리자, 수정)
- **Gemini API 키 입력칸** 추가 (경로 탭 헤더, localStorage `ss_gemini_key` — dict_manager와 별도 키). dict_manager의 callGemini 패턴 재활용(gemini-2.5-flash, JSON 모드).
- **경로 카드에 [✨ AI 자동 채우기] 버튼**: 진료명 보고 `SS_FILL_SYS`로 생성 →
  · synonyms 8~15개 → kw-chips(유의어)에 중복 제외 추가
  · cue_any **12~20개**(부위어 변형·통증어 변형·구어 조합, "너무 좁히지 말 것") → cue-chips(증상 단서)
  · wiki_title/wiki_text → 위키칸 (**비어있을 때만** 채움, 기존 입력 보호)
  · 의료광고법(제56조) SYS: 최고/유일/완벽/무통/완치/부작용없음 금지, 중립표현.
- **경로 카드에 증상 단서(cue) 칩 입력칸** 추가 (위키 칸 아래). 기존 자동 rule의 단서 로드해 표시.
- **[이 경로 저장] 시 증상규칙 자동 upsert**: 이 경로를 단독 paths로 갖는 dm_ss_symptom rule을 찾아 갱신/생성(label=위키 제목). 단서 비우면 그 자동 rule 삭제. (탭2의 복합 규칙·painwords는 그대로 공존)
- **자동완성 경량 인덱스 생성**: 빌드 끝에 `search_suggest.json` 생성. 키워드(진료명·유의어) + 증상단서(dm_ss_symptom rule)를 `{term,name,url,type:keyword|symptom}`로. 빌드 리포트에 suggest_count.
- (기존) dm_dict 역인덱스 머지 코드 잔존 — related 있는 용어만, 무해.

### search_smart.php (검색 결과, 수정)
- **search_symptoms.php 폴백 완전 제거** (`include` 삭제) → 증상규칙은 dm_ss_symptom(DB)에서만 로드.
- **사전 정의 카드(ss-dict) 추가**: search_index.json의 `dict_terms` 표시 (위키 카드 다음, teal 색).
- **증상 매칭 느슨화 — `ss_canon()` 동의어 정규화 추가**:
  · 부위어: 이빨/치아/어금니/앞니/윗니/아랫니 → '이'
  · 통증어: 아파요/아픔/쑤셔/쑤시 → '아파', 욱신거려/찌릿 → '욱신', 시려요/시린/시림 → '시려'
  · 증상 단서·pain_words 매칭에 ss_canon 적용 → "이빨이 아파요" = "이가 아파요" 매칭.
- **애매하면 후보 안내**: 1위 매칭이 짧은 단서(_score ≤ 2)이고 매칭 2개 이상이면 exact 단독 대신 suggest(후보 여러 개). 안내 문구 강화: "'{q}'은(는) 여러 진료와 관련될 수 있어요. 이 중 어디에 가까우신가요?" (검색어 반영).

### search_box.php (검색창, 신규 교체)
- 기존 단순 검색창 → **자동완성 미리보기** 추가.
- 타이핑 시 `search_suggest.json` 읽어 드롭다운: canon 정규화(search_smart와 동일), 양방향 부분일치, 진료🦷/증상💬 태그 구분, ↑↓ 키보드 네비, Enter 선택/Esc 닫기, 매칭 없으면 "엔터로 통합검색" 안내.
- 선택 시 해당 진료로 이동, 또는 엔터로 search_smart 통합검색.

### dict.php (용어사전 사용자 프론트, 신규)
- dm_dict를 ㄱ~ㅎ / A~Z(용어 있을 때만) 색인 탭으로. 빈 초성은 흐리게 비활성.
- 모드: index(가나다 300개) | list(?ini=초성) | search(?q=, term·definition·synonyms LIKE) | detail(?term=).
- 상세: 정의 + 상세설명(wiki_text) + 다른 표현(synonyms) + **관련 진료 링크**(related → search_index.json으로 진료명·URL 매핑, 사전↔진료 양방향).
- head_sub/head/tail로 감쌈, setting.css 톤(teal/gold).

---

## 3. dict.php 500 에러 — 교훈 (중요)

- **증상**: dict.php 첫 배포 시 500.
- **원인**: `mysqli_query($db, ...)`로 `$db` 변수를 직접 사용. 그누보드 환경에서 `$db`가 본문 스코프에 안 잡혀 `mysqli_query(null,...)` → 치명적 에러.
- **해결**: 그누보드 표준 래퍼로 교체 — `sql_query()` / `sql_fetch()` / `sql_fetch_array()` / `sql_real_escape_string()`. (search_smart.php가 쓰는 검증된 방식)
- **규칙**: 스마트서치/사전 프론트에서 DB 접근은 **항상 sql_* 래퍼** 사용. `$db` 직접 쓰지 말 것. 페이지 타이틀은 `$g5['title']` 설정.

---

## 4. 데이터 흐름 (전체 그림)

```
[관리] smartsearch_manager
  ├─ 경로 등록(dm_ss_path) + AI 자동채우기 → dm_ss_keyword(유의어·위키) / dm_ss_symptom(증상규칙)
  └─ [인덱스 빌드] → search_index.json (검색 본문)  +  search_suggest.json (자동완성 경량)

[관리] dict_manager → dm_dict (용어·정의·유의어·related)

[사용자] search_box(자동완성) → search_smart(결과: 위키·사전정의·후보안내)
[사용자] dict.php (용어사전 색인·검색·상세 → 관련진료 링크)
```

---

## 5. 적용 순서 (테스트 → 정리)

1. 4개 파일 업로드: smartsearch_manager.php, search_smart.php, search_box.php, dict.php
2. smartsearch_manager에서 Gemini 키 입력 → 경로별 [AI 자동 채우기] → [이 경로 저장] (유의어·위키·증상 + 증상규칙 생성)
3. **[인덱스 빌드]** 실행 → search_index.json + search_suggest.json 생성 확인 (리포트에 suggest_count)
4. 검색 테스트:
   - "이가 아파요" / "이빨이 아파요" → 같은 후보 안내(정규화 확인)
   - 자동완성: 검색창에 "이빨" 타이핑 → 미리보기 드롭다운
   - dict.php → 색인·검색·상세·관련진료 링크
5. 정상 확인 후 삭제: **search_supplement.php · search_symptoms.php · build_index.php · wiki_test.php**
6. search_box.php를 메인·헤더에 include / dict.php를 메뉴·푸터에 링크(`/sub/smartsearch/dict.php`)

---

## 6. 파일 정리

| 파일 | 상태 |
|---|---|
| smartsearch_manager.php | 유지(수정) — 관리자 |
| dict_manager.php | 유지 — 관리자 |
| search_smart.php | 유지(수정) — 검색 결과 |
| search_box.php | 유지(신규) — 검색창+자동완성 |
| dict.php | 유지(신규) — 용어사전 프론트 |
| search_supplement.php | **삭제 가능** — dm_ss_keyword로 이전, 참조 0 |
| search_symptoms.php | **삭제 가능** — dm_ss_symptom으로 이전, 폴백 제거됨 |
| build_index.php | **삭제 가능** — 빌드 흡수 |
| wiki_test.php | **삭제 가능** |

---

## 7. 다음 후보

- 증상 단서/유의어 운영하며 매칭 품질 튜닝 (canon 사전 확장).
- dict.php ↔ search_smart 상호 링크 강화(검색 결과에서 "용어사전에서 보기" 등).
- 다른 병원 사이트에 동일 구조 이식 (테이블·폴더 공용).
